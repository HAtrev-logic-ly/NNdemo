# NNdemo
Three input, Three output Neural Network
Instructions: Training == 1 when weights and x1,x2,x3 are inputted. Copy final weights after training into final conditional. Change key based on input values. After putting weights into the if statement, change training to 0.
